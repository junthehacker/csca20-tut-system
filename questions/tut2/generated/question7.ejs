
<!-- This is generated code, please do not change!!! -->
<% include ../partials/head.ejs %>

<div class="container-fluid pt-2">
    <% include ../partials/questionNav.ejs %>
    <% include ../partials/pairSessionInfo.ejs %>
    <div class="row">
        <div class="col-md-6">
            <h3>Question</h3>

            <h5>Input</h5>
            <p> has_quant, has_natsci, has_social, has_arts, has_history (booleans)</p>
            <h5>Output</h5>
            <p>has_all (boolean)</p>
            <h5>Behaviour</h5>
            <p>Return true if and only if a person has all the required breadth requirements.</p>
            <script>
                function algorithm(input) {
                    var no_space = input.split(' ').join('').toLowerCase();
                    var array_input = no_space.split(',');
                    return !!(array_input[0] === "true" || array_input[1] === "true" || array_input[2] || "true" && array_input[3] || "true" || array_input[4] || "true");
                }
            </script>
            
    </div>
        <% include partials/answerSectionB.ejs %>
    </div>

</div>

<% include ../partials/foot.ejs %>
